OPENAI_API_KEY=
DEEPSEEK_API_KEY=
OPENROUTER_API_KEY=
GITHUB_TOKEN=
INDEXNOW_KEY=
# Person B (content generation) — optional; keyless runs use template fallback:
ANTHROPIC_API_KEY=
# CITED_MODEL=claude-sonnet-5

# Hexclave (auth + payments + deployments) — optional locally:
# `npm run dev` uses the Hexclave CLI onboarding flow with no keys.
HEXCLAVE_PROJECT_ID=
HEXCLAVE_SECRET_SERVER_KEY=
NEXT_PUBLIC_HEXCLAVE_PROJECT_ID=
