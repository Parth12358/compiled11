Person A — retrieval. Run 20–30 category queries, capture URLs that come back as citations, aggregate by domain, flag where the client is absent. Uses OpenAI Responses API (web_search tool with structured url_citation annotations) as primary. DeepSeek as secondary via OpenAI SDK (no native web search — regex URL extraction from model output). OpenRouter disabled (too slow). Concurrency-limited (4 workers), retry on 5xx/429/timeout with exponential backoff, disk cache. Output: sources + score. STATUS: DONE.

Person B — action. Audit the client site (4 parallel HTTP fetches: HTML, robots.txt, llms.txt, sitemap), diff against gap keywords from A, generate metadata rewrite + landing page + llms.txt + robots.txt fixes via DeepSeek-v4-pro (4 parallel LLM calls). PR creation decoupled — available standalone via pr.ts. Output: keyword gap list + downloadable metadata file (cited-metadata.txt). Hard deadlines: 90s retrieve, 90s generate. STATUS: DONE.

Person C — dashboard. Owns the entire demo surface, starts from the fixture at minute zero, never blocked once. Most Beautiful is still live and this is where it's won. Hexclave for client login is the $1000, and it's an honest use. STATUS: NOT STARTED.
