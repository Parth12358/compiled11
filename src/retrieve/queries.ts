// Category query templates (20-30)
// Seeded from client homepage metadata, interpolated per run
export const QUERY_TEMPLATES: string[] = [];

export function buildQueries(category: string, keywords: string[]): string[] {
  return [];
}
